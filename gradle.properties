# Semver shown in-game and in packaged builds (jpackage --app-version). Bump via run-scripts/distribute.sh.
version=0.0.1

org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.warning.mode=all
